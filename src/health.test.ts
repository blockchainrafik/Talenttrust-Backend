/**
 * Smoke test – kept for historical compatibility.
 * Full route coverage lives in src/integration.test.ts.
 */
describe('health smoke', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});
